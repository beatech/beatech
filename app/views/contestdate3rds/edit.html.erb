<h2><%= @order %>組の日程・会場</h2>

<%= form_for(@contestdate3rd) do |f| %>
  <% if @contestdate3rd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contestdate3rd.errors.count, "error") %> prohibited this contestdate from being saved:</h2>

      <ul>
      <% @contestdate3rd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
  <div class="field">
    <p>会場・日程</p>
    <%= f.text_field :place, class: 'span7' %>
  </div>
  <br />
  <div class="actions">
    <%= f.submit '編集', class: 'btn' %>
  </div>
<% end %>


<%= link_to 'Back', root_url + 'contest3rd' %>

