<%= form_for(@contestdate3rd) do |f| %>
  <% if @contestdate3rd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contestdate3rd.errors.count, "error") %> prohibited this contestdate3rd from being saved:</h2>

      <ul>
      <% @contestdate3rd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order %><br />
    <%= f.number_field :order %>
  </div>
  <div class="field">
    <%= f.label :place %><br />
    <%= f.text_field :place %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
