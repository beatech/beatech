.achievements
  - if @current_user
    %h3 報告フォーム
    = render "achievements/form", action: :create

  - else
    %h3 成果報告とは
    %p 部員の音ゲーの成果を報告し合い、交流を深めたり、モチベーションを高め合ったりするためのページです。

  = render "achievement_list", achievements: @achievements
  = paginate @achievements, window: 5, theme: "twitter-bootstrap"
